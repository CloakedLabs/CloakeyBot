export default class {
    logo: string;
    name: string;
    url: string;

    constructor(logo: string, name: string, url: string){
        this.logo = logo;
        this.name = name;
        this.url = url;
    }
}